# LinScreen

Комбайн для Linux: снимки экрана, запись видео и анимаций, редактор аннотаций.
Работает в системном трее, опирается на готовые системные компоненты
(FFmpeg, PulseAudio/PipeWire, xdg-desktop-portal) вместо собственных кодеков.

## Запуск

Готовый файл `dist/linscreen` запускается двойным щелчком и не требует ни
Python, ни Qt, ни установленного ffmpeg — всё вложено внутрь. Главного окна
нет: значок появляется в системном трее, одиночный щелчок по нему делает
снимок выделенной области.

Проверка работоспособности: `dist/linscreen --check` выводит найденный
FFmpeg, доступные кодеки, звуковые источники и состояние составных частей.

Ярлык в меню приложений и автозапуск включаются галочками в
«Настройки → Общие», вручную ничего прописывать не нужно.

## Сборка единого файла

```bash
./build.sh
```

Сценарий создаёт окружение, загружает сборку FFmpeg 8.1 и собирает
`dist/linscreen` (около 140 МБ). Вложенная сборка FFmpeg выбрана с
поддержкой PulseAudio и SVT-AV1, зависит только от glibc и работает в
любом современном дистрибутиве. Облегчённый вариант, использующий
системный ffmpeg, собирается так:

```bash
LINSCREEN_BUNDLE_FFMPEG=0 ./build.sh
```

## Запуск из исходных текстов

```bash
sudo apt install ffmpeg libxcb-cursor0
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python main.py
```

## Возможности

| Возможность | Состояние |
|---|---|
| Снимок области, всего экрана, активного окна, отдельного монитора | Готово |
| Форматы снимков PNG / JPEG / WEBP / BMP с настройкой качества | Готово |
| Формат AVIF | Требует `pillow-avif-plugin` |
| Редактор: стрелка, рамка, карандаш, маркер, размытие, текст, нумератор | Готово |
| Отмена и возврат правок, копирование в буфер, сохранение | Готово |
| Запись MKV / MP4 / WebM / MOV / AVI, профили H.264, H.265, AV1, VP9, FFV1, ProRes | Готово |
| Запись области, всего экрана либо отдельного монитора | Готово |
| Звук: системный, микрофон, микс, раздельные дорожки с подписями | Готово |
| Ручная настройка кодека, CRF или битрейта, пресета и аргументов FFmpeg | Готово |
| Скрытие окон программы во время записи | Готово |
| Отключение всплывающих уведомлений без потери журнала | Готово |
| Настройки в читаемом файле с примечаниями, импорт и экспорт | Готово |
| Полностью ручная команда FFmpeg с подстановками | Готово |
| Анимации: GIF (два прохода с палитрой), Animated WebP, APNG | Готово |
| Пауза записи: фрагменты со склейкой без перекодирования | Готово |
| Корректное завершение по «q», аварийное сохранение материала | Готово |
| Снимок экрана в Wayland через портал | Готово |
| Запись экрана в Wayland через портал ScreenCast | Реализовано, см. ограничения |
| Глобальные клавиши в X11 | Готово |
| Глобальные клавиши в Wayland | Требуется портал GlobalShortcuts |
| Быстрый выбор источника звука из меню трея | Готово |
| Журнал работы FFmpeg для диагностики | Готово |
| Автозапуск при входе в систему | Готово |

### Ограничения записи в Wayland

Путь «портал ScreenCast → PipeWire → FFmpeg» реализован, но для работы
требуются два условия:

* бэкенд портала с интерфейсом `ScreenCast` — пакеты `xdg-desktop-portal-gnome`,
  `-kde`, `-wlr` или `-hyprland` (сборки `-gtk` и `-xapp` его не содержат);
* FFmpeg версии 7.1 или новее — в нём появился фильтр `pipewiregrab`.

При отсутствии любого из условий пункты записи в меню остаются недоступными,
а приложение сообщает конкретную причину. Снимки экрана в Wayland работают
через портал `Screenshot` без дополнительных условий.

## Настройка кодирования

Вкладка «Кодирование» в настройках уточняет выбранный профиль: энкодер
видео и звука, постоянное качество (CRF) либо заданный битрейт, пресет
скорости, интервал ключевых кадров, формат пикселей и произвольные
дополнительные аргументы.

Наверху вкладки показано название выбранного профиля и заданные им
значения, а в каждом поле вариант «как в профиле» дополнен конкретным
значением в скобках. Внизу выводится итоговая команда, которая будет
выполнена: она меняется вместе с настройками, поэтому результат виден
до начала записи.

Флажок «Использовать свою команду» передаёт сборку команды пользователю
целиком. В образце доступны подстановки:

| Подстановка | Чем заменяется |
|---|---|
| `{ffmpeg}` | путь к кодировщику вместе с общими флагами |
| `{video_input}` | аргументы захвата экрана выбранной области |
| `{audio_input}` | аргументы всех выбранных источников звука |
| `{output}` | путь к файлу результата |
| `{fps}`, `{width}`, `{height}` | параметры захвата по отдельности |

Кнопка «Подставить текущую команду» заполняет поле готовым образцом,
соответствующим выбранному профилю, — его остаётся только поправить.
Обозначение `{output}` обязательно, иначе команда отклоняется до начала
записи. Флаги передачи прогресса и остановки по «q» подставляются
принудительно, поэтому счётчик и кнопка «Стоп» работают и с ручной
командой.

## Редактор аннотаций

Открывается сразу после снимка, если это не отключено в настройках.
Крупный снимок вписывается в окно, масштаб меняется колесом мыши.

| Действие | Клавиши |
|---|---|
| Выбор инструмента | `1`…`7` в порядке кнопок на панели |
| Удалить выбранную аннотацию | `Delete` |
| Отменить, вернуть | `Ctrl+Z`, `Ctrl+Shift+Z` |
| Копировать результат и закрыть | `Ctrl+C` |
| Сохранить и закрыть | `Ctrl+S` |
| Сохранить как | `Ctrl+Shift+S` |
| Закрыть без сохранения | `Esc` |

Готовые аннотации перемещаются мышью. Размытие необратимо уничтожает
содержимое выбранной области, а не закрывает его сверху.

## Горячие клавиши

| Действие | Сочетание по умолчанию |
|---|---|
| Снимок области | `Print` |
| Снимок всего экрана | `Shift+Print` |
| Снимок активного окна | `Alt+Print` |
| Запись области: начать и остановить | `Ctrl+Alt+R` |
| Пауза записи | `Ctrl+Alt+P` |

Запись начинается и останавливается **одним и тем же сочетанием**: во
время съёмки окна программы скрыты, и запоминать вторую комбинацию не
приходится. Остановить запись можно и через меню значка в трее.

Изменяются в «Настройки → Клавиши». Перехват в X11 не монопольный: если
клавиша уже занята другой программой (например, Flameshot или штатным
снимком экрана рабочего стола), сработают обе. В этом случае комбинацию
следует сменить либо освободить в конфликтующей программе.

## Структура

```
core/     настройки, определение сессии, хоткеи, автозапуск, фоновые вызовы
vendor/   статическая сборка ffmpeg для вложения в исполняемый файл
capture/  захват экрана (x11grab, Qt, grim), порталы D-Bus, звуковые устройства
encoder/  профили FFmpeg, поиск бинарника, контроллер процессов, сохранение картинок
editor/   графический холст аннотаций
ui/       трей, оверлей выделения, панель записи, настройки, журнал
tests/    модульные проверки на unittest
app.py    связывание модулей в рабочий цикл
main.py   точка входа, режимы --check и --version
linscreen.spec  описание сборки единого исполняемого файла
```

## Файл настроек

Настройки хранятся в `~/.config/linscreen/config.ini` — обычном текстовом
файле с заголовками разделов и примечанием к каждому значению:

```ini
# Параметры записи экрана
[video]
# Профиль записи, например mkv_h264, mp4_h264, webm_vp9, gif
profile_id = mkv_h264
# Частота кадров захвата
fps = 30
# Записывать указатель мыши: да или нет
show_cursor = да
```

Править файл можно вручную любым редактором. Кнопки «Экспорт настроек…»
и «Импорт настроек…» на вкладке «Общие» переносят его целиком, что
позволяет держать несколько наборов или переносить настройки между
машинами. Повреждённый файл не мешает запуску: неверные значения
отбрасываются, а недостающие берутся по умолчанию. Настройки прежнего
формата `config.json` переносятся автоматически при первом запуске.

Запись автозапуска создаётся в `~/.config/autostart/linscreen.desktop`
и ссылается на текущий исполняемый файл.

## Поведение во время записи

Флажок «Скрывать окна программы во время записи» на вкладке «Общие»
убирает с экрана панель записи и все открытые окна программы, чтобы они
не попали в кадр. Управление остаётся через значок в трее и горячие
клавиши; после остановки окна возвращаются.

Флажок «Показывать всплывающие уведомления» отключает только всплывающие
окна. Сообщения продолжают записываться в журнал и в файл журнала.

## Диагностика

Пункт «Журнал…» в меню трея показывает команды, переданные FFmpeg, и его
вывод; он же дублируется в файл `~/.cache/linscreen/linscreen.log`.
Необработанные ошибки записываются в `~/.cache/linscreen/crash.log`.
Там видно точную причину отказа записи. Окно настроек на вкладке
«Общие» показывает найденный бинарник FFmpeg, его версию и перечень
недостающих возможностей сборки.

## Проверка кода

```bash
.venv/bin/python -m unittest discover -s tests -t .   # модульные тесты
.venv/bin/python -m flake8 .                          # стиль
.venv/bin/python -m mypy .                            # типы
```
